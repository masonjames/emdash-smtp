# emdash-smtp-core

Internal shared runtime for EmDash SMTP.

If you are installing EmDash SMTP in an app, install `emdash-smtp` for trusted setups or `emdash-smtp-marketplace` for sandboxed setups instead.
